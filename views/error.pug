doctype html
html
    head
        title View Poll - Pollster
        include css.pug
    body
        include navbar.pug
        div.container-fluid
            div(class=['row', 'vcenter'])
                div(
                    class=['col-md-4','col-md-offset-4', 'poll-container']
                )
                    p(class=['block-title', 'text-center']) Oops!
                    br
                    p.text-center Looks like we couldn't find that page!
                    
                    if message
                        p= message
                    
        include footer.pug
        include js.pug